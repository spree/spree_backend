<script type='text/template' id='product_template'>
  <li id="product_{{ product.id }}" data-classification-id='{{ classification.id }}' data-product-id='{{ product.id }}' class="product p-2 col-6 col-md-3 col-lg-2">
    <div class="m-1 p-2 card bg-light">
      <nav class="navbar navbar-light bg-light justify-content-between px-0 pt-0" style="background-color:transparent !important;">
        <a class=" sort-handle js-sort-handle">
          <%= svg_icon name: "move.svg", width: '22', height: '22' %>
        </a>
        <div class="dropdown p-0">
          <button class="btn btn-light dropdown-toggle products-ddb btn-sm" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= svg_icon name: "chevron-down.svg", width: '14', height: '14' %>
          </button>
          <div class="dropdown-menu dropdown-menu-right p-2" aria-labelledby="dropdownMenuButton">
            <a href="{{ edit_product_url product.id }}" class="edit-product js-edit-product dropdown-item p-1"><%= Spree.t(:edit) %></a>
            <a href="javascript:;" class="delete-product js-delete-product dropdown-item p-1"><%= Spree.t(:remove) %></a>
          </div>
        </div>
      </nav>
      <div class='image'>
        {{#if image }}
          <div class="admin-product-image-container">
            <img src='{{image}}' alt='{{ product.attributes.name }}' class='thumbnail rounded border border' />
          </div>
        {{ else }}
          <div class="admin-product-image-container">
            <%= inline_svg_tag "noimage/backend-missing-image.svg", class: "noimage", size: "60%*60%" %>
          </div>
        {{/if}}
      </div>
      <div class="p-0 pt-3 text-center product-info">
        <h6 class="name card-text">{{ product.attributes.name }}</h6>
        <p class="price card-text">{{ product.attributes.display_price }}</p>
      </div>
    </div>
  </li>
</script>
