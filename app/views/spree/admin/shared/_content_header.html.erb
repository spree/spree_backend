<div id="contentHeader" class="row mb-0 pb-0 mt-4">
  <% if content_for?(:page_title) || content_for?(:page_actions) %>
    <div class="content-header col <%= "with-page-tabs" if content_for?(:page_tabs) %> page-header">
      <div id="contentHeaderRow" class="row align-items-center  <%= if content_for?(:page_tabs) then "pb-3" else "pb-3 mb-3" end %>">

        <% if content_for?(:page_title) %>
          <h2 class="px-0 mb-0 col">
            <%= yield :page_title %>
            <% if content_for?(:page_title_small) %>
              <small><%= yield :page_title_small %></small>
            <% end %>
          </h2>
        <% end %>

        <% if content_for?(:page_actions) %>
          <div class="page-actions pl-0 pr-0 d-none d-lg-flex" data-hook="toolbar">
            <%= yield :page_actions %>
          </div>
        <% end %>

        <% if content_for?(:page_actions) %>
          <div class="d-flex d-lg-none justify-content-end m-0 p-0">
            <button type="button"
                    id="contextual-menu-toggle"
                    class="btn btn-outline-secondary">
                    <span class="d-none d-sm-inline">
                      <%= Spree.t('more') %>
                    </span>
                    <%= svg_icon name: "chevron-down.svg", width: '16', height: '16' %>
            </button>
          </div>
        <% end %>
      </div>

      <% if content_for?(:page_tabs) %>
        <div class="row my-2 mb-3 pb-1 pt-1 align-items-center" data-nav-x-wrapper>
          <div data-nav-x-container class="w-100">
            <ul id="spreePageTabs" class="nav nav-pills flex-nowrap" data-nav-x-content>
              <%= yield :page_tabs %>
            </ul>
          </div>

          <%= render partial: 'spree/admin/shared/ui_elements/side_scroll_buttons' %>
        </div>
      <% end %>

    </div>
  <% end %>
</div>
